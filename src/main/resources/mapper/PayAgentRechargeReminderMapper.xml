<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiqilm.server.admin.mapper.PayAgentRechargeReminderMapper">

    <resultMap type="PayAgentRechargeReminder" id="PayAgentRechargeReminderResult">
        <result property="id"    column="id"    />
        <result property="type"    column="type"    />
        <result property="content"    column="content"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
    </resultMap>

    <sql id="selectPayAgentRechargeReminderVo">
        select id, type, content, create_time ,update_time, status from pay_agent_recharge_reminder
    </sql>

    <select id="selectPayAgentRechargeReminderList" parameterType="PayAgentRechargeReminder" resultMap="PayAgentRechargeReminderResult">
        <include refid="selectPayAgentRechargeReminderVo"/>
        <where>
            <if test="type != null  and type != ''"> and type = #{type}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="status != null "> and status = #{status}</if>
        </where>
    </select>

    <select id="selectPayAgentRechargeReminderById" parameterType="Long" resultMap="PayAgentRechargeReminderResult">
        <include refid="selectPayAgentRechargeReminderVo"/>
        where id = #{id}
    </select>

    <insert id="insertPayAgentRechargeReminder" parameterType="PayAgentRechargeReminder" useGeneratedKeys="true" keyProperty="id">
        insert into pay_agent_recharge_reminder
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="type != null">type,</if>
            <if test="content != null">content,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="status != null">status,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="type != null">#{type},</if>
                <if test="content != null">#{content},</if>
                <if test="createTime != null">#{createTime},</if>
                <if test="updateTime != null">#{updateTime},</if>
                    <if test="status != null">#{status},</if>
             </trim>
    </insert>

    <update id="updatePayAgentRechargeReminder" parameterType="PayAgentRechargeReminder">
        update pay_agent_recharge_reminder
        <trim prefix="SET" suffixOverrides=",">
            <if test="type != null">type = #{type},</if>
    
            <if test="content != null">content = #{content},</if>
    
            <if test="createTime != null">create_time = #{createTime},</if>
    
            <if test="updateTime != null">update_time = #{updateTime},</if>
    
            <if test="status != null">status = #{status},</if>
    
        </trim>
        where id = #{id}
    </update>

    <delete id="deletePayAgentRechargeReminderById" parameterType="Long">
        delete from pay_agent_recharge_reminder where id = #{id}
    </delete>

    <delete id="deletePayAgentRechargeReminderByIds" parameterType="String">
        delete from pay_agent_recharge_reminder where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
